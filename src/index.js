import React from "react";
import ReactDOM from "react-dom";

function App() {
  return "See index.test.js";
}

const rootElement = document.getElementById("root");
ReactDOM.render(<App />, rootElement);
